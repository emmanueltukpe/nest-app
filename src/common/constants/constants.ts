export const connections: Connection = {
    CONNECTION_STRING: 'String',
    DB: 'SQL',
    DB_NAME: 'Database'
};
export type Connection = {
    CONNECTION_STRING: string;
    DB: string;
    DB_NAME: string;
}